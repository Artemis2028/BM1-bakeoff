# HTML catalog remastered-lock

`HTML_CATALOG_LOCKED_FROM_REMASTERED === false`

This static page set is implemented from bake-off `docs/` + the landed weapon ledger (PRs #48 / #49) + in-repo JSON only. Flash is **source evidence**, not a required viewer. Do **not** `git am` remastered patches. Do **not** crib `BM1-remastered-work`.

Companion to the banners on `index.html`, `weapons.html`, and `stations.html`.

Brief **#54 stay-locked**. No Referee Pass claimed.
